name: Build Android APK

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Debug folder structure
        run: |
          echo "Current directory:"
          pwd
          echo ""
          echo "Files and folders:"
          ls -la

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.44.6'
          channel: 'stable'

      - name: Install dependencies
        run: flutter pub get -C shoptrack_clean

      - name: Build APK (debug)
        run: flutter build apk --debug --no-tree-shake-icons --android-skip-build-dependency-validation -C shoptrack_clean

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: shoptrack_clean/build/app/outputs/flutter-apk/app-debug.apk
